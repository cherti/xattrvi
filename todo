TODO:
* implement command-line-option to set "follow_symlinks" to False
  to be able to edit properties of symlinks
* implement parsing .xattrvirc to allow for ignored user-fields
  -> "ignore: my.key.should.go-here"
* implemend command-line-flag to toggle displaying user-uneditable
  attributes (should default to "don't display")
* move away from filesystemencoding to default to utf-8
* catch encode-errors and display something like "unknown encoding" in the file
  (and test on that when rereading) or display unreadable keys as commented out
  (option to show or not to show for xattrvirc? force-encode for xattrvirc?)
* option to specify encoding to use
* catch encoding-errors and ignore that line

* implement checksumming in xattrs for importaint files
  -> note the secured files and provide easy way to recompute and verify integrity

* xattrvirc:
  -> ignore defined keys
  -> set encoding for defined keys
  -> ~/.xattrvirc or ~/.config/xattrvi/xattrvirc?
